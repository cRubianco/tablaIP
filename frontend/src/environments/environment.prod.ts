/**
 * configuracion productiva, cuando no tiene server, toma el  mismo de donde esta alojada la app
 */
  export const environment = {
    production: true,
    SERVER: "http://172.20.99.6:3100"
  } 