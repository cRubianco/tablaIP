// { nro, address, grupo, user, pcname, dependency, opersystem, observ, type, other } 
export interface Address {
  nro: string;
  address: string;
  grupo: string;
  user: string;
  pcname: string;
  dependency: string;
  opersystem: string;
  observ: string;
  type: string; 
  other: string;
  _id: string;
}